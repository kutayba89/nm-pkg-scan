# NM PKG Scan

A lightweight static web app for package scanning and floor lookup.

## Run locally

```powershell
python -m http.server 8000
```

Open `http://localhost:8000` in your browser.

## GitHub Pages

To publish this site on GitHub Pages, enable Pages for the repository and serve from the `main` branch root.
